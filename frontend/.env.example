# Copy to .env.local for local dev. Never commit the real .env.local.

# Base URL of the serving API (src/electricity_demand/serving/app.py).
# Defaults to http://localhost:8080 if unset - see the root README.md for
# how to run that locally, or set this to the real deployed Cloud Run URL:
# https://electricity-demand-serving-vi7c6rjegq-uc.a.run.app
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
